import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // App Router is default in Next.js 13+; no extra config needed.
  // Keeping this file explicit for Vercel to auto-detect framework.
};

export default nextConfig;
